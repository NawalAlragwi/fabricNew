#  BCMS — Scenario S1: SHA-256 Only
#  Hyperledger Caliper Benchmark Configuration
#  Branch: final | Research Paper Scenario Comparison
#
#  S1 Description:
#    Pure SHA-256 on-chain hashing. SHA-256 is the performance BASELINE.
#    MagnificationFactor = 3000 in chaincode -> 45ms CPU cost per tx.
#    Bottlenecks much earlier than BLAKE3 (S2) due to sequential hashing.
#
#  FIX (v13.0 / final branch):
#    payloadSize changed from 50000 to 5000 to match S2 for fair comparison.
#    The 10x payload asymmetry in the original config masked the algorithmic
#    performance difference between SHA-256 and BLAKE3.
#
#  Expected Results (after fix):
#    IssueCertificate write:  ~50-80 TPS (SHA-256 MAG=3000 is the bottleneck)
#    Avg Latency (write):     ~1.5-2.5 s
#    BLAKE3 (S2) TPS will be >30% higher (approaching 200+ TPS)
#

monitors:
  resource:
    - module: docker
      options:
        interval: 1
        containers:
          - orderer.example.com
          - peer0.org1.example.com
          - couchdb0
          - peer0.org2.example.com
          - couchdb1
          - ca_org1
          - ca_org2
          - ca_orderer

test:
  name: bcms-S1-sha256-stress-benchmark-final
  description: >
    Scenario S1: SHA-256-only hashing stress test (final branch).
    MagnificationFactor=3000 in chaincode => ~45ms CPU per tx.
    Linear ramp 100->1000 TPS on IssueCertificate to find the CPU bottleneck.
    Expected: SHA-256 saturates at ~50-80 TPS.
    BLAKE3 (S2) achieves >30% higher TPS due to MAG=500 (2ms/tx vs 45ms/tx).
  workers:
    type: local
    number: 10

  rounds:
    # ────────────────────────────────────────────────────────────────────
    # Round 1: IssueCertificate — SHA-256 Write Stress
    # linear-rate: ramps from 100 TPS to 1000 TPS over 60s
    # Workers: 5 (SHA-256 saturates early; more workers = queue backpressure)
    # batchSize: 1 (single cert per tx — pure SHA-256 overhead measurement)
    # ────────────────────────────────────────────────────────────────────
    - label: IssueCertificate
      description: >
        S1 (final): SHA-256 write stress. Linear ramp 100->1000 TPS.
        SHA-256 MAG=3000 => 45ms CPU/tx => bottlenecks at ~50-80 TPS.
        payloadSize=5000 (5KB) - same as S2 for fair comparison.
        MVCC-safe: CERT_{worker}_{index} unique keys, idempotent on duplicate.
      txDuration: 60
      rateControl:
        type: linear-rate
        opts:
          startingTps: 100
          finishingTps: 1500
      workload:
        module: workload/issueCertificate.js
        arguments:
          batchSize: 1
          payloadSize: 5000
      txOptions:
        invokerIdentity: User1@org1.example.com

    # ────────────────────────────────────────────────────────────────────
    # Round 2: VerifyCertificate — Read at 1000 TPS (no hash overhead)
    # Demonstrates full network read capacity without SHA-256 bottleneck
    # ────────────────────────────────────────────────────────────────────
    - label: VerifyCertificate
      description: >
        S1: Read-only verify at 1000 TPS. SHA-256 recomputed client-side
        for hash comparison. readOnly=true bypasses orderer.
        Shows network read capacity unconstrained by write CPU bottleneck.
      txDuration: 30
      rateControl:
        type: fixed-rate
        opts:
          tps: 100
      workload:
        module: workload/verifyCertificate.js
      txOptions:
        invokerIdentity: User1@org1.example.com

    # ────────────────────────────────────────────────────────────────────
    # Round 3: QueryAllCertificates — CouchDB at 1000 TPS
    # ────────────────────────────────────────────────────────────────────
    - label: QueryAllCertificates
      description: >
        S1: CouchDB rich query at 1000 TPS. readOnly=true.
        No hashing overhead — pure query throughput measurement.
      txDuration: 30
      rateControl:
        type: fixed-rate
        opts:
          tps: 100
      workload:
        module: workload/queryAllCertificates.js
      txOptions:
        invokerIdentity: User1@org1.example.com

    # ────────────────────────────────────────────────────────────────────
    # Round 4: RevokeCertificate — Write at 1000 TPS (Org2 RBAC)
    # ────────────────────────────────────────────────────────────────────
    - label: RevokeCertificate
      description: >
        S1: Revocation write at 1000 TPS. Org2MSP RBAC.
        No hash computation in revoke path — tests write throughput without hash.
        Idempotent: nil on cert-not-found or already-revoked.
      txDuration: 30
      rateControl:
        type: fixed-rate
        opts:
          tps: 100
      workload:
        module: workload/revokeCertificate.js
      txOptions:
        invokerIdentity: User1@org2.example.com

    # ────────────────────────────────────────────────────────────────────
    # Round 5: GetCertificatesByStudent — CouchDB Composite Index at 1000 TPS
    # ────────────────────────────────────────────────────────────────────
    - label: GetCertificatesByStudent
      description: >
        S1: Student query at 1000 TPS. CouchDB composite index.
        readOnly=true. Zero hash overhead.
      txDuration: 30
      rateControl:
        type: fixed-rate
        opts:
          tps: 100
      workload:
        module: workload/getCertificatesByStudent.js
      txOptions:
        invokerIdentity: User1@org1.example.com

    # ────────────────────────────────────────────────────────────────────
    # Round 6: GetAuditLogs — Audit Query at 1000 TPS
    # ────────────────────────────────────────────────────────────────────
    - label: GetAuditLogs
      description: >
        S1: Audit log query at 1000 TPS. Returns [] in benchmark mode.
        readOnly=true. Demonstrates audit trail query capacity.
      txDuration: 30
      rateControl:
        type: fixed-rate
        opts:
          tps: 100
      workload:
        module: workload/getAuditLogs.js
      txOptions:
        invokerIdentity: User1@org1.example.com
