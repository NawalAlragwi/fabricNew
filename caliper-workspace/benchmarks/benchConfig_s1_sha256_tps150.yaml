# ============================================================
#  S1 — SHA-256 Baseline  |  Target TPS: 150
#  VerifyCertificate: 30 → 150 TPS (PRIMARY ROUND)
# ============================================================

monitors:
  resource:
    - module: docker
      options:
        interval: 1
        containers:
          - orderer.example.com
          - peer0.org1.example.com
          - couchdb0
          - peer0.org2.example.com
          - couchdb1

test:
  name: bcms-s1-sha256-tps150
  description: >
    Scenario S1 SHA-256 Baseline at 150 TPS target.
    VerifyCertificate: linear 30→150 TPS (PRIMARY COMPARISON ROUND).
    Magnification x1500 = 22,500 us hash overhead per tx.
  workers:
    type: local
    number: 3

  rounds:

    - label: IssueCertificate
      description: Issue certs at 37 TPS, 60s
      txDuration: 60
      rateControl:
        type: fixed-rate
        opts:
          tps: 37
      workload:
        module: workload/issueCertificate.js
        arguments:
          contractId: bcms-sha256
          payloadSize: 20000
      txOptions:
        invokerIdentity: User1@org1.example.com

    # ── الجولة الأساسية للمقارنة ──────────────────────────────────────
    - label: VerifyCertificate
      description: >
        PRIMARY ROUND — SHA-256 linear 30→150 TPS, 120s.
        Re-computes SHA-256 on-chain per call: 22,500 us CPU overhead.
        Expected: latency climbs steeply as TPS rises (no SIMD).
      txDuration: 120
      rateControl:
        type: linear-rate
        opts:
          startingTps: 30
          finishingTps: 150
      workload:
        module: workload/verifyCertificate.js
        arguments:
          contractId: bcms-sha256
          totalIssued: 1000
      txOptions:
        invokerIdentity: User1@org1.example.com

    - label: QueryAllCertificates
      description: I/O-bound query 30 TPS, 60s (algorithm has zero impact)
      txDuration: 60
      rateControl:
        type: fixed-rate
        opts:
          tps: 30
      workload:
        module: workload/queryAllCertificates.js
        arguments:
          contractId: bcms-sha256
      txOptions:
        invokerIdentity: User1@org1.example.com

    - label: RevokeCertificate
      description: Flag-only revoke 30 TPS, 60s (no hash recomputation)
      txDuration: 60
      rateControl:
        type: fixed-rate
        opts:
          tps: 30
      workload:
        module: workload/revokeCertificate.js
        arguments:
          contractId: bcms-sha256
          totalIssued: 1000
      txOptions:
        invokerIdentity: User1@org1.example.com

    - label: HashOnlyBenchmark
      description: Pure CPU hash isolation at 120 TPS, 60s
      txDuration: 60
      rateControl:
        type: fixed-rate
        opts:
          tps: 120
      workload:
        module: workload/hashOnlyBenchmark.js
        arguments:
          contractId: bcms-sha256
          payloadSize: 20000
      txOptions:
        invokerIdentity: User1@org1.example.com
