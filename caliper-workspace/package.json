{
  "name": "caliper-workspace-blake3",
  "version": "1.0.0",
  "description": "Hyperledger Caliper Benchmark — BCMS BLAKE3 Branch (fabric-blake3-new)",
  "private": true,
  "scripts": {
    "generate-report": "node generate_custom_report.js",
    "benchmark": "npx caliper launch manager --caliper-workspace . --caliper-networkconfig networks/networkConfig.yaml --caliper-benchconfig benchmarks/benchConfig.yaml --caliper-flow-only-test",
    "install-blake3": "npm install blake3 --ignore-scripts"
  },
  "dependencies": {
    "@hyperledger/caliper-cli": "0.6.0",
    "fabric-network": "^2.2.19",
    "blake3": "^0.3.3"
  }
}