name: Caliper-BLAKE3
version: \2.0.0caliper:
  blockchain: fabric
channels:
  - channelName: mychannel
    contracts:
      - id: basic
organizations:
  - mspid: Org1MSP
    identities:
      certificates:
        - name: 'User1@org1.example.com'
          clientPrivateKey:
            path: '\'
          clientSignedCert:
            path: '\'
    connectionProfile:
      path: 'networks/connection-org1.yaml'
      discover: false
  - mspid: Org2MSP
    identities:
      certificates:
        - name: 'User1@org2.example.com'
          clientPrivateKey:
            path: '\'
          clientSignedCert:
            path: '\'
    connectionProfile:
      path: 'networks/connection-org2.yaml'
      discover: false
